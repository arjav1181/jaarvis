"""W18 self-improvement loop — Friday review over the week's failures.

The loop mines three evidence sources (red tests, failed cron runs, refused
acts), drafts kanban fix cards with PR contracts, proposes curator trims for
dead/unused skills, and files a cited weekly report. Dry-run by default;
live mode needs an explicit operator flag per run (see run.py).
"""
